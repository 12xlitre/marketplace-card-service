# Marketplace Card Service

## Идея

Внутренний сервис для компании, которая ведет клиентов-продавцов на маркетплейсах.

Проблема: нельзя сразу брать в работу большое количество карточек магазина, но малый объем карточек часто не устраивает клиентов. Пример запроса: 1000 карточек.

Предполагаемая задача сервиса:
- заходить в магазин/кабинет клиента;
- получать информацию по всем товарным позициям;
- массово готовить и вносить изменения по согласованию;
- работать с заголовками, описаниями, ключами и другими полями карточек.

## Формат продукта

Сервис должен быть визуальным рабочим пространством для технического специалиста, а не набором разрозненных скриптов.

Предполагаемый формат:
- отдельный кабинет/веб-приложение, возможно на поддомене;
- внутри кабинета специалист видит клиентов, магазины, карточки, задачи, статусы и результаты;
- действия по массовой обработке карточек должны быть управляемыми через интерфейс;
- сервис должен помогать контролировать путь от загрузки данных до согласования и публикации изменений.

## Текущий прототип

Frontend переведен на React + Vite. Исходники лежат в `frontend/`, production artifact собирается single-file в корневой `index.html`, чтобы текущий prod Dockerfile продолжал работать без изменения серверной обвязки.

Прод-адрес:

```text
https://opticards.weboptai.ru/
```

Health:

```text
https://opticards.weboptai.ru/healthz
```

Локальная ссылка после запуска:

```text
http://localhost:5173/
```

Команда запуска:

```bash
npm run prototype
```

Команды frontend:

```bash
npm run frontend:dev
npm run frontend:build
npm run frontend:check
```

В прототипе есть:
- экран логина через backend: пользователи в SQLite, пароли в PBKDF2-хешах, сессия в HttpOnly cookie;
- read-only backend-подключение WB API для загрузки списка карточек через `/api/wb/cards`;
- главный экран `Кабинеты` с демо WB кабинетом, реальными кабинетами из backend, фильтром активных/неактивных и добавлением портала;
- левое меню `Кабинеты`, `Аудит`, `Настройки`;
- переход в кабинет селлера/портала;
- обзор кабинета: карточки, карточки к проверке, честный маршрут работы, участники проекта;
- состав проекта из серверного списка пользователей;
- добавление портала через API или вручную; WB API ключ отправляется только в backend, проверяется read-only запросом и сохраняется зашифрованно;
- режимы охвата работы: полный магазин и выбранные карточки;
- визуальный маршрут загрузка -> аудит -> правки -> согласование -> обновление;
- таблица карточек, статусы, ошибки WB и быстрые действия.
- детальная карточка с вкладками `Карточка`, `Аудит`, `Изменения`;
- вкладка `Аудит` принимает необязательные ссылки/nmID конкурентов именно для текущего аудита, не смешивая их с отдельным блоком `ТОП конкурентов`;
- вкладка `Отчеты` внутри кабинета селлера с первым клиентским WB XLSX-отчетом и явным выбором периода перед выгрузкой;
- ручной черновик "Было / Стало" без обязательного аудита: заголовок, описание, характеристики;
- редактирование характеристик через чипсы значений в колонке `Стало`, с лимитами WB `maxCount/charcType`, подсказками и возможностью удалить/добавить значения;
- пометки источников подсказок характеристик: строгий справочник WB, подсказки из карточек, свободное значение.

## Следующий шаг

Ответы пользователя на вопросы из `docs/discovery.md` разобраны и внесены в discovery.

Текущий продуктовый шаг: довести качество backend-аудита с MPStats до стабильного рабочего инструмента и открыть быстрый сценарий добавления магазина без WB API — по ссылке на магазин или карточки.

После read-only подключения WB: развить массовый режим вокруг детальной карточки - взять выбранные карточки в работу, пакетно запустить аудит, подготовить черновики `Было / Стало`, согласовать и выгрузить результат.

Важное решение: публичные тестовые учетные записи убраны из frontend. После стабилизации auth продолжаем подключать реальный WB API только на чтение и затем MPStats API для анализа.

Основной discovery-документ: `docs/discovery.md`.

## Статус на 2026-07-13

Сделано:
- Клиентский WB XLSX-отчет перенесен во вкладку `Отчеты` внутри кабинета селлера, чтобы будущие отчеты добавлялись в контексте конкретного кабинета.
- В UI отчета добавлен сценарий `выбрать отчет -> выбрать период дата начала / дата конца -> сформировать`; файл больше не собирается сразу на зашитые `8 недель` из примера.
- Внутри каждого кабинета сохраняется локальная история последних сформированных отчетов: тип отчета, период, дата формирования, имя файла и возможность сформировать повторно с тем же периодом.
- Backend `GET /api/portals/{id}/wb-client-report` принимает `start/end` (`YYYY-MM-DD`), валидирует диапазон до 90 дней и считает прошлый период такой же длины для сравнения; `weeks` оставлен как fallback совместимости.

Проверки:
- `python3 -m py_compile server.py`;
- `npm run frontend:check`;
- `npm run frontend:build`;
- `git diff --check`.

## Статус на 2026-07-09

Сделано:
- СЯ отделено от аудита: `POST /api/card-audit` больше не возвращает `semanticCore` в `auditResult/summary`, а сводка `Изменения -> Контент` больше не показывает карточку СЯ; семантика остается отдельной вкладкой детальной карточки.
- `POST /api/card-audit` расширен необязательным полем `auditCompetitors`: frontend отправляет до 3 ссылок/nmID из вкладки `Аудит`, backend проверяет коммерческую схожесть, принимает релевантные ручные карточки с приоритетом и добирает итоговый список конкурентов до 5 через MPStats.
- Добавлен прозрачный протокол выбора конкурентов для аудита: `manualAccepted`, `manualRejected`, `autoSelected`, `finalCompetitors`, summary-счетчики и причины выбора/отклонения сохраняются в `auditEntry/evidenceSummary`; вкладка `Аудит` показывает принятых, отклоненных и автодобранных конкурентов после запуска.
- Скоринг конкурентов усилен товарными признаками: тип изделия, пол/сегмент, возрастный сегмент, материал, близость цены и попадание в поисковый спрос карточки; ручные конкуренты с явным конфликтом сегмента получают понятную причину отклонения.
- Добавлено явное обнуление работы по кабинету из UI: в блоке `Источник данных` появилась кнопка `Обнулить работу`, которая после подтверждения удаляет аудиты, черновики, рабочий набор и историю согласования, но сохраняет WB API-ключ и заново подтягивает карточки.
- Backend `POST /api/portals/{id}/reset-work-cache` расширен: кроме `card_drafts` теперь чистит `portal_workset_cards` и `card_approval_events`; все удаления идут после `user_can_access_portal` и фильтруются по `portal_id`.
- Прод задеплоен коммитом `4a2c6f9`; `https://opticards.weboptai.ru/healthz` отвечает `ok`, серверная рабочая копия на commit `4a2c6f9`.

Проверки:
- `python3 -m py_compile server.py`;
- `npm run frontend:check`;
- `npm run frontend:build`;
- `git diff --check`.

Что осталось:
- Для сценария Насти по СЯ нужен отдельный MPStats endpoint именно для `SEO -> Расширение запросов`, а не текущий `/analytics/v1/wb/items/{nmID}/keywords`; публичная база знаний подтверждает поля отчета (`Запрос`, `Кластер WB`, `Приоритетный предмет`, `Частота WB`), но публичный API-метод в документации пока не найден.
- Метод найден в SPA MPStats и проверен с нашим `X-Mpstats-TOKEN`: `POST /api/seo/keywords/expanding/create-report`, затем polling `GET /api/seo/keywords/expanding/{reportHash}`. Тест по `пижама женская` вернул 5000 запросов и 59 приоритетных предметов.
- Вкладка `Семантическое ядро` переключена на MPStats SEO expansion: стартовый запрос, фильтр по приоритетному предмету, поиск по найденным запросам, левая область выбранных/действующих запросов, правая область найденных запросов, выгрузка выбранного СЯ в XLSX.
- После сбора СЯ frontend автоматически выбирает фильтр по предмету карточки, если MPStats вернул совпадающий `prioritySubject` (например, `Одежда / Пижамы`), чтобы широкие нерелевантные запросы не были первыми в выдаче.
- 2026-07-09 исправлена ложная ошибка UI `MPStats SEO expansion не загрузился. Проверьте ключ`: ключ и endpoint проверены на prod, backend polling увеличен, frontend теперь показывает конкретную причину (`отчет еще готовится`, `ключ не подключен`, либо сообщение MPStats). Прод commit `f5214bf`, health `ok`; контрольный запрос `пижама рибана женская` вернул 4883 запроса.
- 2026-07-09 исправлен UX выбора СЯ: левая колонка переименована в `Выбранные запросы`, кнопка `В работу` теперь переносит запрос в интерфейсе оптимистично и отдельно показывает статус сохранения в черновик. Прод commit `e0888e2`, health `ok`.
- 2026-07-09 добавлена история подборок СЯ в черновик карточки: хранится дата создания, стартовый запрос, фильтры, выбранные запросы и результат MPStats; старую подборку можно открыть обратно и скачать выбранную выборку XLSX. Добавлено поле `Слова исключения`: фильтрует правую колонку без нового запроса MPStats, включая простые словоформы (`шорты` скрывает `шортами`). Прод commit `5be5bcd`, health `ok`.
- 2026-07-09 исправлена кнопка `В работу` во вкладке СЯ: выбранный запрос теперь переопределяет такой же запрос из действующих, новые выбранные показываются сверху, лимит выбранной выборки увеличен до 2000, левая колонка показывает до 500 выбранных с выгрузкой полного списка в XLSX. Прод commit `6048a8a`, health `ok`.
- 2026-07-09 метрики вкладки СЯ сделаны динамическими: `Действующие` считает левую колонку вместе с добавленными, `Выбранные` переименовано в `Добавленные`, `Всего MPStats` пересчитывается по текущему предмету/поиску/словам исключения, а не показывает статичные 5000. Прод commit `0078e77`, health `ok`.
- 2026-07-09 уточнены счетчики СЯ: отдельно показываются `По фильтрам` и полный `Отчет MPStats`, заголовок правой колонки показывает точное `к добавлению`, а список найденных запросов раскрывается кнопками `Показать еще`/`Показать все` внутри прокручиваемого блока. Прод commit `2f8ac41`, health `ok`.
- 2026-07-09 исправлена выгрузка СЯ: кнопка скачивания больше не передает click-event вместо строк, левая колонка UI переименована в `Выбранные и добавленные`, XLSX теперь содержит 2 столбца — `Действующие до подбора` и `Добавленные в работу`. Прод commit `7ee3e7d`, health `ok`.
- 2026-07-10 добавлена первая функция после сбора СЯ: кнопка `Переоптимизировать` во вкладке `Семантическое ядро` вызывает GigaChat/LLM через `POST /api/card-content-reoptimize`, переписывает заголовок и описание по выбранным запросам и сохраняет результат в черновик вкладки `Изменения` без write-операций в WB.
- 2026-07-10 вкладка `ТОП конкурентов` переведена с пустого ручного списка на рабочий MPStats-сценарий: `POST /api/card-competitors/suggest` подбирает до 5 коммерчески похожих карточек, сохраняет их в `card_competitors`, показывает источник, причину выбора, продажи/выручку, разницу цены, описание и характеристики относительно текущей карточки.

## Статус на 2026-07-08

Сделано:
- auth-поток переключен в backend-only: логин/восстановление сессии через `/api/login`, `/api/session`, `/api/logout`.
- сессии теперь живут 12 часов, с `remember me` — 7 дней; сервер выставляет `HttpOnly`-cookie и обнуляет ее на logout.
- обновлен список сотрудников для интерфейса состава проекта: данные берутся из `/api/users`, при ошибке API fallback из `document/test-users.csv`.
- выбор сотрудника в блоке проекта фильтруется по роли (`Руководитель`, `Технический специалист`, `Менеджер`), кнопка добавить начинает работать после загрузки сотрудников.
- добавлен read-only WB API client: `POST /content/v2/get/cards/list` через backend, cursor-пагинация, timeout/retry на transient/429, `withPhoto: -1`, нормализация карточек для интерфейса.
- добавлен route `GET /api/wb/cards?portal_id=...&limit=...`, он требует backend-сессию и не принимает токен из браузера.
- добавлен route `POST /api/portals`: создает портал из UI, для режима API принимает WB ключ только в теле запроса, проверяет read-only доступ, сохраняет токен зашифрованно и возвращает нормализованные карточки.
- добавлен route `GET /api/portals`: после логина frontend получает список кабинетов из SQLite, а `localStorage` остается только fallback при недоступном backend.
- добавлен route `POST /api/portals/<portal_id>/team`: состав проекта сохраняется в backend `portal_members`.
- счетчики карточек/правок/проблем и `last_sync_at` сохраняются на уровне портала после WB read-only загрузки.
- добавлена команда `npm run wb-sync -- --portal-id demo-wb --limit 20` для серверной проверки WB без frontend.
- встроенный первый кабинет в frontend теперь пытается загрузить реальные карточки WB после логина; без токена показывает состояние `API ожидает токен` и остается на демонстрационных строках.
- форма `Добавить портал` теперь включает поле WB API ключа в API-режиме и не сохраняет ключ в localStorage; при ошибке WB/backend показывает сообщение в модальном окне.
- изменения `f213cd0` запушены в `origin/main`, создан backup tag `backup/pre-wb-readonly-deploy-20260708-084312`.
- Сергей переключил prod runtime на Python backend `server.py`; `/api/session` отвечает `200`, `/document/test-users.csv` отвечает `404`, SQLite живет в `/opt/opticards/var`.
- `OPTICARDS_SECRET_KEY` уже хранится в `/opt/opticards/.env.prod` с правами `600`; заново его не генерировать.
- backend/read-only изменения зафиксированы в коммитах `f213cd0`, `8362a6c`, `cc38150`.
- через интерфейс подключен реальный WB кабинет: портал `1`, integration `wb:connected`, токен зашифрован в SQLite, read-only загрузка возвращает 4 карточки.
- при API-подключении ручной ввод названия портала скрыт; название определяется по WB-данным: бренд, если есть, иначе доминирующая категория.
- frontend переведен на React + Vite single-file build, root `index.html` генерируется из `frontend/`.
- детальная карточка теперь показывает расширенный WB snapshot: бренд, описание, характеристики, размеры, габариты, фото и `rawFields`; пустые значения явно показываются как `Пусто`.
- backend sanitizes `rawFields` перед выдачей в UI и вырезает ключи с признаками секретов.
- изменения задеплоены в prod коммитом `ce19e2e`; прод проверен через `GET /`, `GET /api/session`, `GET /api/portals` без сессии -> `401`.
- повторное API-подключение WB кабинета блокируется: `POST /api/portals` возвращает `portal_already_connected` или `portal_already_archived`, проверяя digest токена и fingerprint `nmID`.
- детальные характеристики показываются рабочим списком и чипсами значений, а служебные поля WB спрятаны в раскрывающийся блок.
- внутри детальной карточки добавлены вкладки: `Карточка`, `Аудит`, `Изменения`;
- вкладка `Изменения` работает без запуска аудита: специалист может вручную менять заголовок, описание и характеристики, а предложения аудита в будущем должны только подсвечиваться как рекомендации;
- черновик изменений сохраняется на backend в `card_drafts` как структурированный payload v2, а `localStorage` остается fallback для демо/недоступного backend;
- согласование правок хранится в `card_drafts.meta.approval`, события переходов статуса дополнительно пишутся в `card_approval_events`; route `GET /api/approval-workflow?portal_id=...` отдает текущие задачи, историю и простую аналитику по кабинету;
- обычная кнопка `Загрузить свежие данные` вызывает soft reset анализа: `POST /api/portals/{id}/reset-analysis-cache` очищает MPStats-кэш и помечает audit-черновики как `stale`, но сохраняет содержимое черновика и задачи согласования;
- MPStats заложен как глобальная интеграция сервиса: один общий ключ хранится зашифрованно в `service_integrations` и должен действовать на все кабинеты селлеров и карточки;
- добавлена проверка MPStats-соединения: backend расшифровывает ключ только на время запроса, вызывает официальный `GET /api/analytics/v1/wb/items/{id}` с заголовком `X-Mpstats-TOKEN`, сохраняет статус `verified/auth_error/rate_limited/error`, но не возвращает ключ во frontend;
- добавлен первый слой MPStats для характеристик: по кнопке в детальной карточке backend создает отчет `/analytics/v1/wb/characteristics-analysis`, забирает результат по hash, нормализует значения по названиям характеристик и подмешивает их в существующие чипсы-подсказки без отдельной громоздкой панели;
- выгрузки из вкладки `Изменения` переведены с CSV на XLSX: отдельные файлы `Контент`, `Цены`, `Остатки`; остатки имеют первый лист в WB-формате FBS `Баркод` / `Количество`, цены вынесены в отдельный лист `Цены и скидки`, контент выгружается одной строкой с характеристиками в колонках и справочным листом по charcID;
- характеристики в `Стало` редактируются чипсами: исходные значения из `Было` предзаполнены, каждый чип можно удалить, новые значения добавляются из подсказок или вручную, если характеристика не строгая;
- для характеристик учитываются `maxCount` и `charcType`: для `charcType = 4` лимит считается как 1 значение, для `maxCount > 0` показывается `X из N`;
- backend route `GET /api/wb/characteristics?portal_id=...&subject_id=...` загружает характеристики категории WB и возвращает `charcID`, `name`, `required`, `maxCount`, `charcType`, `hasFilter`, `isVariable`, `existNamedField`;
- для части строгих WB-справочников подключены directory-подсказки: цвет, пол/гендер, страна производства, сезон; для пола/сезона есть fallback-значения на случай пустого ответа WB directory;
- для остальных характеристик UI явно показывает, что это не полный список WB, а `подсказки из карточек`; свое значение разрешено, если характеристика не помечена как строгий справочник;
- важное ограничение WB API: официальный `/content/v2/object/charcs/{subjectId}` не отдает полный список допустимых значений для произвольной характеристики вроде `Фактура материала` или длины; полный список ЛК WB для таких полей публичным API не найден;
- принято решение вернуться к расширенному словарю значений после получения MPStats API: MPStats/внешние данные могут стать источником подсказок по категории, а ручной ввод специалистов нужно будет накапливать в своем словаре `subjectID + charcID`.
- публичные фильтры WB-каталога обсуждались как возможный обходной источник значений, но не выбраны как основа: это не seller API, может быть нестабильно и требовать отдельного маппинга фильтр -> `charcID`; предпочтительный путь — MPStats API.
- список кабинетов и WB-эндпоинты усилены проверкой доступа: пользователь видит только свои/доступные порталы, глобальный доступ есть у admin/all/руководителя, `/api/wb/cards` и `/api/wb/characteristics` проверяют доступ к `portal_id` до получения токена;
- WB token meta пересчитывается от реального `iat` JWT-токена: срок считается от даты выпуска + 180 дней, старые сохраненные токены декодируются на backend из encrypted storage без отдачи токена во frontend.
- экран списка карточек развит в рабочую поверхность: поиск по карточкам, фильтры по проблемности/статусу/категории, явные причины проверки, счетчики и ограниченный рабочий набор карточек с сохранением выбора в `localStorage` по кабинету.
- уточнена семантика таблицы карточек: отсутствие бренда в WB считается справочным сигналом, но не снижает заполненность и не создает задачу; рабочий статус в таблице отделен от автоматических сигналов WB.
- таблица карточек теперь учитывает backend-задачи согласования из `/api/approval-workflow`: колонка `Работа` показывает `на согласовании`, `на доработке`, `принято`, `в наборе` или `нет задачи`, а локальный рабочий набор больше не подменяет реальные задачи.
- в кабинете селлера убрана правая колонка: `Состав проекта` и компактный `Контур безопасности` перенесены наверх горизонтальным блоком, чтобы таблицы карточек и задач использовали всю ширину рабочей области.
- в детальной карточке порядок вкладок изменен под рабочий сценарий: сначала фактическая `Карточка`, затем `Аудит`, затем `Изменения`; новая карточка открывается на фактических данных.
- рабочий набор карточек перенесен в backend `portal_workset_cards`; из выбранных карточек можно создать внутренний массовый пакет задач, который создает `card_drafts` со статусом `draft` (`в работе`) без write-запросов в WB.
- вкладка `Карточка` в детальной карточке получила фактический снимок: фото, заголовок, описание, ключевые поля, размеры/габариты, баркод, цены/скидки при наличии в WB snapshot.
- получен и изучен пакет Сергея `document/OptiCards.rar`: README, integration spec, pipeline pseudocode, LLM system prompt, JSON schema и примеры input/output для карточного аудита WB.
- добавлен backend route `POST /api/card-audit`: собирает `evidenceBundle` из WB snapshot/WB CDN/WB характеристик/MPStats, строит структурированный `auditResult` по схеме OptiCards/MP Audit, возвращает draft-предложения для заголовка, описания и характеристик; при недоступности внешних источников возвращает частичный аудит с `riskNotes`.
- вкладка `Аудит` в детальной карточке переключена с frontend stub на backend-аудит: результат сохраняется в `card_drafts.meta.auditResult/evidenceSummary`, история аудита показывает engine, MPStats-сигналы, главные выводы, быстрые правки и ограничения ручной проверки.
- добавлен опциональный LLM refinement для аудита через OpenAI-compatible `/chat/completions`, если настроены `OPTICARDS_LLM_API_KEY`/`OPENAI_API_KEY`; без ключа работает deterministic audit по фактам без галлюцинаций.
- сопоставление характеристик WB и MPStats ужесточено: спорные совпадения по слабому token overlap больше не принимаются, поэтому `Тип ростовки` не должен получать значения `Тип карманов`, а `Размер на модели` не должен получать значения `Материал`.
- на вкладке `Карточка` в детальной карточке убран дубль фотографии: основное фото остается в боковом превью, а внутри фактической вкладки больше не повторяется вторым изображением.
- предупреждения аудита очищены от технических сообщений MPStats: ошибки вида `Поле path обязательно` больше не показываются пользователю напрямую, а заменяются понятной причиной про недоступный рыночный контекст ниши.
- старые сохраненные audit-черновики и `localStorage` теперь тоже очищаются от технических MPStats warnings перед показом: `3ca09a2`.
- предупреждения ручной проверки уточнены: `Не удалось выбрать конкурентов из MPStats subject/items` схлопывается с причиной про нишу, а MPStats-доли характеристик показываются как статистическая подсказка, не как ошибка: `eb7f3e0`.
- найдена реальная причина MPStats `path`: короткий `/analytics/v1/wb/items/{nmID}` не отдает категорию, а `/analytics/v1/wb/items/{nmID}/full` отдает `subject.id`; для карточки `505512309` это `subject.id = 162`, `subject.name = "Одежда / Пижамы"`.
- для MPStats endpoints `/analytics/v1/wb/subject/items`, `/analytics/v1/wb/subject/brands`, `/analytics/v1/wb/subject/price_segmentation` backend теперь использует `path = subject.id` из `/items/{id}/full` или fallback `subjectID`, передает `path` только в query, а для season effects использует `path`, не `subject_id`: `f043e40`.
- списочные ответы MPStats для `price_segmentation` и `season_effects` больше не отбрасываются: `9cbd4d1`.
- текст методики аудита во frontend упрощен: вместо `нет источника — нет утверждения` теперь говорится, что рекомендации строятся только на доступных данных WB, MPStats и конкурентов, а при нехватке данных пункт уходит в ручную проверку: `dce0c0f`.
- действие массового набора переименовано в UI: `Создать пакет` -> `Взять в работу`, статусы `Берем в работу`, `взято в работу`, `не удалось взять в работу`: `ff9374b`.
- изменения задеплоены на прод: `64a2a6f`, `6b0859e`, `c7cb538`, `8345c6a`, `3ca09a2`, `eb7f3e0`, `f043e40`, `9cbd4d1`; `https://opticards.weboptai.ru/healthz` отвечает `ok`, backend-контейнер `opticards-web-1` запущен.
- прод-проверка по карточке `505512309` после фикса: `warnings_raw []`, `nichePathMissing False`, `nicheItems 80`, `brands 30`, `priceSegmentation list 25`, `season list 12`.

Что осталось:
- первоочередно на 2026-07-09: добавить сценарий заведения второго магазина по ссылке на магазин WB и/или списку ссылок на карточки, без WB API-ключа;
- разобрать MPStats-источники для такого сценария: как из ссылки магазина/карточки получить `sellerId`, `nmID`, текущие публичные данные карточки, предмет, характеристики, цену, рейтинг, фото, конкурентов и SEO/ключи;
- на базе MPStats сделать загрузку карточек в кабинет, чтобы аудит и `Взять в работу` работали одинаково для WB API и link-based порталов;
- развить массовые задачи: выбранные карточки -> `Взять в работу` -> пакетный запуск аудита -> список черновиков `Было / Стало` -> согласование;
- допилить LLM refinement: подключить ключ `OPTICARDS_LLM_API_KEY`/`OPENAI_API_KEY` на проде, проверить, что LLM улучшает тексты только поверх evidence и не выдумывает факты;
- заново запустить аудит карточки `505512309` через UI, чтобы новый результат перезаписал старый сохраненный draft с прежними `riskNotes`;
- проверить качество нового `POST /api/card-audit` на реальных карточках с MPStats-ключом: особенно соответствие характеристик, корректность категории, качество ключей/заголовков/описаний и понятность предупреждений ручной проверки;
- при необходимости добавить явный маппинг названий WB `charcID` <-> MPStats-отчет, если строгого сопоставления по названиям будет недостаточно;
- после MPStats решить модель словаря значений: `subjectID + charcID + source` (`wb_directory`, `mpstats`, `portal_cards`, `manual`);
- сверить XLSX-выгрузку контента с реальным категорийным шаблоном WB из ЛК, потому что WB формирует точные контент-шаблоны под предмет/категорию.

Алерты и ограничения:
- `AGENTS.md` в репозитории отсутствует, триггеры doc-update выполняем вручную по измененным зонам (`auth`, `REST`, `frontend`, `маршруты`);
- не вести две версии "демо" и "деплой": текущий боевой контур `opticards.weboptai.ru` является рабочим демо для команды;
- перед выкладкой фиксировать изменения в git и сохранять понятную точку отката;
- прод-обвязка (`Dockerfile`, `docker-compose.prod.yml`, `deploy/`) root-owned на сервере и не добавляется в git;
- код на сервере не редактировать, только `push main` -> `sudo opticards-deploy`;
- до отдельного решения никаких write-операций в WB не выполняется.
- env fallback `WB_API_TOKEN` разрешен только для `demo-wb`; числовые порталы должны использовать encrypted token через `set-wb-token`, чтобы не смешивать кабинеты.
- на локальной машине Chromium для Playwright установлен без sudo: браузер лежит в `~/.cache/ms-playwright`, недостающие библиотеки распакованы в `/home/dmitriy/.local/chromium-deps/root`; для запуска нужен `LD_LIBRARY_PATH=/home/dmitriy/.local/chromium-deps/root/usr/lib/x86_64-linux-gnu:$LD_LIBRARY_PATH`.

Action items за пользователем:
- подготовить на 2026-07-09 ссылку на магазин WB и несколько ссылок на карточки для проверки link-based заведения магазина;
- вручную прогнать аудит нескольких реальных карточек на проде и отметить странные рекомендации по характеристикам/категориям/текстам;
- передать/подтвердить LLM API-ключ для prod, если хотим включить LLM-переформулировку;
- передать MPStats API-ключ/документацию, если потребуется расширить рыночный контекст или уточнить endpoints;
- подтвердить сценарий участия клиентов в авторизации/согласовании.

## Статус на 2026-07-07

Сделано:
- обработаны ответы технического специалиста по процессу Wildberries;
- в `docs/discovery.md` добавлены фактический процесс работы с новым/существующим магазином, источники данных, аудит карточки, предложения по улучшениям, массовое обновление, роли, согласование и интерфейсные требования;
- добавлены реальные материалы: пример выгрузки WB, кейсы "было/стало" по заголовкам и описанию, ссылки на хорошую и плохую карточку;
- проверены официальные материалы Wildberries/WB API по карточкам, токенам, лимитам, ошибкам и ограничению наименования 60 символов;
- уточнено, что все предложения требуют ручной проверки, менеджер не применяет изменения, а технический специалист является основным пользователем публикации;
- выбран и реализован следующий экран прототипа: детальная карточка товара.

Frontend:
- в `index.html` добавлен экран детальной карточки товара;
- из таблицы карточек открывается детальный экран через кнопку с иконкой просмотра;
- на детальном экране есть аудит проблем, заполненность, варианты заголовка с лимитом 60 символов, блок "было/стало", ручное редактирование, характеристики, риски цены, backup и sticky-блок действий;
- добавлен возврат из детальной карточки к таблице карточек.
- добавлен экран входа первым экраном прототипа;
- палитра прототипа переведена ближе к корпоративным цветам WebOptimize;
- создана тестовая таблица пользователей `document/test-users.csv` для раннего прототипа;
- вход сделан устойчивее: есть встроенный fallback тестовых учеток и кнопки автозаполнения логина/пароля;
- 2026-07-08 публичная тестовая таблица и автозаполнение убраны; добавлен backend `server.py` с SQLite-пользователями, PBKDF2-хешами паролей и HttpOnly-сессиями;
- демо-кабинет WB помечен как `read-only`, кнопки создания задач, массовых правок и применения изменений отключены.
- левое меню прототипа приведено к новой структуре: `Кабинеты`, `Аудит`, `Настройки`;
- главный экран кабинетов оставлен с одним демо-кабинетом и рабочим действием `Добавить портал`;
- добавлено модальное окно добавления портала с режимами `Через API` и `Вручную`; статический прототип сохраняет только метаданные портала, API-ключ в браузере не сохраняется;
- внутри кабинета добавлен обзор: карточки, карточки в работе, проблемы, участники проекта;
- добавлен блок состава проекта с выбором участников из тестовых пользователей;
- добавлены placeholder-экраны `Аудит` и `Настройки` под будущую настройку категорий, СЯ и интеграций.
- первая версия прототипа залита в GitHub: ветка `main`, remote `origin`, первый коммит `dc55a69` (`Initial opticards prototype`);
- добавлен `.gitignore` для локального мусора, зависимостей, сборок, логов и `.env`-файлов.
- зафиксирован сценарий добавления магазина: на главном экране выбор "Через API" или "Ручной режим"; внутри кабинета магазина можно подключить API позже и добавлять таблицы/карточки вручную.
- OptiCards развернут на общем сервере: `https://opticards.weboptai.ru/`, health `https://opticards.weboptai.ru/healthz` отвечает `ok`;
- добавлен `docs/deployment.md` с правилами прод-работы, командами `opticards-deploy/restart/logs/ps` и ограничениями публичных файлов.
- входной сценарий `логин -> кабинеты -> добавление портала -> кабинет портала` закоммичен как `e81bc94` (`Update portal onboarding prototype`), запушен в `origin/main` и задеплоен на `https://opticards.weboptai.ru/`;
- после деплоя проверено: главная страница отдает `HTTP 200`, `https://opticards.weboptai.ru/healthz` отвечает `ok`, контейнер `opticards-web-1` находится в статусе `Up`.

Что доделать следующим:
- довести прод-обвязку до нового backend-сервера и завести пользователей через `npm run create-user`;
- подключить реальный API-ключ WB в режиме только чтения и посмотреть, какие данные реально возвращаются;
- заменить демо-данные кабинета на данные из реального WB API без записи изменений;
- подключить MPStats API для анализа запросов, конкурентов и частотности;
- добавить экран импорта Excel/WB-выгрузки и маппинга колонок;
- затем развивать массовый режим: таблица -> пакет предложений -> построчное утверждение -> публикация -> чек-ап;
- позже перейти от статического прототипа к приложению, когда сценарии стабилизируются.

Алерты и ограничения:
- репозиторий инициализирован и отслеживает `origin/main`;
- прод-обвязка реализована на сервере, но не лежит в этом репозитории; не добавлять сюда `Dockerfile`, `docker-compose.prod.yml`, `deploy/` и edge/frontend-nginx конфиги без отдельного согласования;
- в новой backend-схеме публично должен отдаваться только `index.html` и API; `docs/`, `WORK.md`, `.git` и `document/*.xlsx/*.docx` закрыты;
- если появятся новые публичные ассеты, лучше складывать их в `public/` и согласовать перевод webroot/сборки на сервере;
- API-интеграция WB и MPStats пока не реализована;
- есть пример Excel-выгрузки WB, но реальные шаблоны для всех операций WB еще не разобраны.
- Playwright в проекте не установлен; проверка выполнена через HTML parser и HTTP 200 локального сервера.
- пароли не должны попадать в frontend, CSV или документацию; текущая схема хранит только PBKDF2-хеши в SQLite.
- до отдельного решения система не должна выполнять никакие write-запросы в Wildberries.
- MPStats API будет подключаться после базового read-only подключения WB.
- на 2026-07-07 после деплоя локально остались незакоммиченные документационные изменения: `README.md`, `WORK.md`, `docs/memory/INDEX.md`, `docs/deployment.md`, `docs/memory/ops_production_deployment.md`, `docs/memory/product_store_onboarding.md`; в прод-коммит `e81bc94` вошел только frontend `index.html`.

Action items за пользователем:
- по возможности позже приложить дополнительные шаблоны WB для других массовых операций и реальные ошибки обновления из кабинета/API;
- передать тестовый API-ключ WB, когда будем готовы подключать read-only интеграцию;
- отдельно решить, будет ли продавец/клиент заходить в сервис для согласования.

## Статус на 2026-07-06

Сделано:
- создан `index.html` как постоянная локальная точка входа прототипа;
- добавлен `package.json` с командами `npm run prototype` и `npm start`;
- добавлен `README.md` с адресом `http://localhost:5173/`;
- обновлен `docs/discovery.md`: добавлен сценарий работы не только с полным магазином, но и с выбранными карточками;
- создана память проекта в `docs/memory/`.

Текущий frontend:
- статический прототип без фреймворка;
- главный экран: кабинет сотрудников;
- второй экран: кабинет селлера;
- есть переключение охвата `Полный магазин` / `Выбранные карточки`;
- есть таблица карточек, статусы, маршрут работы и быстрые действия.

Что доделать следующим:
- ответы пользователя на вопросы из `docs/discovery.md` получены и разобраны 2026-07-07;
- следующий экран выбран и реализован 2026-07-07: детальная карточка товара;
- позже решить, когда переходить со статического HTML на полноценное приложение.

Алерты и ограничения:
- коммитов в репозитории пока нет;
- сервер прототипа запускается локально командой `npm run prototype`;
- `AGENTS.md` внутри проекта не найден;
- REST-клиент, auth, хранение данных, API-маршруты и база данных пока не реализованы.

Action items за пользователем:
- принести ответы на вопросы из `docs/discovery.md`;
- по возможности приложить реальные примеры: выгрузку WB, таблицу аудита, хорошую/плохую карточку, ошибки WB.
