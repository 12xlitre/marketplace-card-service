# Marketplace Card Service

## Идея

Внутренний сервис для компании, которая ведет клиентов-продавцов на маркетплейсах.

Проблема: нельзя сразу брать в работу большое количество карточек магазина, но малый объем карточек часто не устраивает клиентов. Пример запроса: 1000 карточек.

Предполагаемая задача сервиса:
- заходить в магазин/кабинет клиента;
- получать информацию по всем товарным позициям;
- массово готовить и вносить изменения по согласованию;
- работать с заголовками, описаниями, ключами и другими полями карточек.

## Формат продукта

Сервис должен быть визуальным рабочим пространством для технического специалиста, а не набором разрозненных скриптов.

Предполагаемый формат:
- отдельный кабинет/веб-приложение, возможно на поддомене;
- внутри кабинета специалист видит клиентов, магазины, карточки, задачи, статусы и результаты;
- действия по массовой обработке карточек должны быть управляемыми через интерфейс;
- сервис должен помогать контролировать путь от загрузки данных до согласования и публикации изменений.

## Текущий прототип

Создан постоянный статический прототип в `index.html`.

Прод-адрес:

```text
https://opticards.weboptai.ru/
```

Health:

```text
https://opticards.weboptai.ru/healthz
```

Локальная ссылка после запуска:

```text
http://localhost:5173/
```

Команда запуска:

```bash
npm run prototype
```

В прототипе есть:
- экран логина через backend: пользователи в SQLite, пароли в PBKDF2-хешах, сессия в HttpOnly cookie;
- главный экран `Кабинеты` с одним демо WB кабинетом и добавлением портала;
- левое меню `Кабинеты`, `Аудит`, `Настройки`;
- переход в кабинет селлера/портала;
- обзор кабинета: карточки, карточки в работе, проблемы, участники проекта;
- состав проекта из серверного списка пользователей;
- добавление портала через API или вручную; API-ключ в статическом прототипе не сохраняется;
- режимы охвата работы: полный магазин и выбранные карточки;
- визуальный маршрут загрузка -> аудит -> правки -> согласование -> обновление;
- таблица карточек, статусы, ошибки WB и быстрые действия.

## Следующий шаг

Ответы пользователя на вопросы из `docs/discovery.md` разобраны и внесены в discovery.

Следующий продуктовый шаг: подключить реальный WB API в режиме только чтения через backend, чтобы заменить демо-данные кабинета на реальные данные и проверить фактический ответ API.

После read-only подключения WB: развить массовый режим вокруг детальной карточки - пакет предложений, построчное утверждение, публикация и чек-ап результата.

Важное решение: публичные тестовые учетные записи убраны из frontend. После стабилизации auth продолжаем подключать реальный WB API только на чтение и затем MPStats API для анализа.

Основной discovery-документ: `docs/discovery.md`.

## Статус на 2026-07-07

Сделано:
- обработаны ответы технического специалиста по процессу Wildberries;
- в `docs/discovery.md` добавлены фактический процесс работы с новым/существующим магазином, источники данных, аудит карточки, предложения по улучшениям, массовое обновление, роли, согласование и интерфейсные требования;
- добавлены реальные материалы: пример выгрузки WB, кейсы "было/стало" по заголовкам и описанию, ссылки на хорошую и плохую карточку;
- проверены официальные материалы Wildberries/WB API по карточкам, токенам, лимитам, ошибкам и ограничению наименования 60 символов;
- уточнено, что все предложения требуют ручной проверки, менеджер не применяет изменения, а технический специалист является основным пользователем публикации;
- выбран и реализован следующий экран прототипа: детальная карточка товара.

Frontend:
- в `index.html` добавлен экран детальной карточки товара;
- из таблицы карточек открывается детальный экран через кнопку с иконкой просмотра;
- на детальном экране есть аудит проблем, заполненность, варианты заголовка с лимитом 60 символов, блок "было/стало", ручное редактирование, характеристики, риски цены, backup и sticky-блок действий;
- добавлен возврат из детальной карточки к таблице карточек.
- добавлен экран входа первым экраном прототипа;
- палитра прототипа переведена ближе к корпоративным цветам WebOptimize;
- создана тестовая таблица пользователей `document/test-users.csv` для раннего прототипа;
- вход сделан устойчивее: есть встроенный fallback тестовых учеток и кнопки автозаполнения логина/пароля;
- 2026-07-08 публичная тестовая таблица и автозаполнение убраны; добавлен backend `server.py` с SQLite-пользователями, PBKDF2-хешами паролей и HttpOnly-сессиями;
- демо-кабинет WB помечен как `read-only`, кнопки создания задач, массовых правок и применения изменений отключены.
- левое меню прототипа приведено к новой структуре: `Кабинеты`, `Аудит`, `Настройки`;
- главный экран кабинетов оставлен с одним демо-кабинетом и рабочим действием `Добавить портал`;
- добавлено модальное окно добавления портала с режимами `Через API` и `Вручную`; статический прототип сохраняет только метаданные портала, API-ключ в браузере не сохраняется;
- внутри кабинета добавлен обзор: карточки, карточки в работе, проблемы, участники проекта;
- добавлен блок состава проекта с выбором участников из тестовых пользователей;
- добавлены placeholder-экраны `Аудит` и `Настройки` под будущую настройку категорий, СЯ и интеграций.
- первая версия прототипа залита в GitHub: ветка `main`, remote `origin`, первый коммит `dc55a69` (`Initial opticards prototype`);
- добавлен `.gitignore` для локального мусора, зависимостей, сборок, логов и `.env`-файлов.
- зафиксирован сценарий добавления магазина: на главном экране выбор "Через API" или "Ручной режим"; внутри кабинета магазина можно подключить API позже и добавлять таблицы/карточки вручную.
- OptiCards развернут на общем сервере: `https://opticards.weboptai.ru/`, health `https://opticards.weboptai.ru/healthz` отвечает `ok`;
- добавлен `docs/deployment.md` с правилами прод-работы, командами `opticards-deploy/restart/logs/ps` и ограничениями публичных файлов.
- входной сценарий `логин -> кабинеты -> добавление портала -> кабинет портала` закоммичен как `e81bc94` (`Update portal onboarding prototype`), запушен в `origin/main` и задеплоен на `https://opticards.weboptai.ru/`;
- после деплоя проверено: главная страница отдает `HTTP 200`, `https://opticards.weboptai.ru/healthz` отвечает `ok`, контейнер `opticards-web-1` находится в статусе `Up`.

Что доделать следующим:
- довести прод-обвязку до нового backend-сервера и завести пользователей через `npm run create-user`;
- подключить реальный API-ключ WB в режиме только чтения и посмотреть, какие данные реально возвращаются;
- заменить демо-данные кабинета на данные из реального WB API без записи изменений;
- подключить MPStats API для анализа запросов, конкурентов и частотности;
- добавить экран импорта Excel/WB-выгрузки и маппинга колонок;
- затем развивать массовый режим: таблица -> пакет предложений -> построчное утверждение -> публикация -> чек-ап;
- позже перейти от статического прототипа к приложению, когда сценарии стабилизируются.

Алерты и ограничения:
- репозиторий инициализирован и отслеживает `origin/main`;
- прод-обвязка реализована на сервере, но не лежит в этом репозитории; не добавлять сюда `Dockerfile`, `docker-compose.prod.yml`, `deploy/` и edge/frontend-nginx конфиги без отдельного согласования;
- в новой backend-схеме публично должен отдаваться только `index.html` и API; `docs/`, `WORK.md`, `.git` и `document/*.xlsx/*.docx` закрыты;
- если появятся новые публичные ассеты, лучше складывать их в `public/` и согласовать перевод webroot/сборки на сервере;
- API-интеграция WB и MPStats пока не реализована;
- есть пример Excel-выгрузки WB, но реальные шаблоны для всех операций WB еще не разобраны.
- Playwright в проекте не установлен; проверка выполнена через HTML parser и HTTP 200 локального сервера.
- пароли не должны попадать в frontend, CSV или документацию; текущая схема хранит только PBKDF2-хеши в SQLite.
- до отдельного решения система не должна выполнять никакие write-запросы в Wildberries.
- MPStats API будет подключаться после базового read-only подключения WB.
- на 2026-07-07 после деплоя локально остались незакоммиченные документационные изменения: `README.md`, `WORK.md`, `docs/memory/INDEX.md`, `docs/deployment.md`, `docs/memory/ops_production_deployment.md`, `docs/memory/product_store_onboarding.md`; в прод-коммит `e81bc94` вошел только frontend `index.html`.

Action items за пользователем:
- по возможности позже приложить дополнительные шаблоны WB для других массовых операций и реальные ошибки обновления из кабинета/API;
- передать тестовый API-ключ WB, когда будем готовы подключать read-only интеграцию;
- отдельно решить, будет ли продавец/клиент заходить в сервис для согласования.

## Статус на 2026-07-06

Сделано:
- создан `index.html` как постоянная локальная точка входа прототипа;
- добавлен `package.json` с командами `npm run prototype` и `npm start`;
- добавлен `README.md` с адресом `http://localhost:5173/`;
- обновлен `docs/discovery.md`: добавлен сценарий работы не только с полным магазином, но и с выбранными карточками;
- создана память проекта в `docs/memory/`.

Текущий frontend:
- статический прототип без фреймворка;
- главный экран: кабинет сотрудников;
- второй экран: кабинет селлера;
- есть переключение охвата `Полный магазин` / `Выбранные карточки`;
- есть таблица карточек, статусы, маршрут работы и быстрые действия.

Что доделать следующим:
- ответы пользователя на вопросы из `docs/discovery.md` получены и разобраны 2026-07-07;
- следующий экран выбран и реализован 2026-07-07: детальная карточка товара;
- позже решить, когда переходить со статического HTML на полноценное приложение.

Алерты и ограничения:
- коммитов в репозитории пока нет;
- сервер прототипа запускается локально командой `npm run prototype`;
- `AGENTS.md` внутри проекта не найден;
- REST-клиент, auth, хранение данных, API-маршруты и база данных пока не реализованы.

Action items за пользователем:
- принести ответы на вопросы из `docs/discovery.md`;
- по возможности приложить реальные примеры: выгрузку WB, таблицу аудита, хорошую/плохую карточку, ошибки WB.
